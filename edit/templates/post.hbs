{{> header}}
    {{#post}}
 <section class="header">
                <h1>{{title}}</h1>
            <p class="post-meta">
                    {{date format="MMMM Do YYYY"}}
                    by <span>{{author.name}}</span>
                    {{#if tags}} on {{{tags}}}{{/if}}
                </p>
 </section>
            <section class="content">
                {{{body}}}
            </section>
    {{/post}}
{{> footer}}
{{> js}}